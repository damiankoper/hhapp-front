language: node_js
services:
  - docker
before_install:
  - docker-compose run vue-dev npm test
script:
  - docker-compose run vue-dev npm test # jest test with coverage flag does coverage too
after_script:
  - 'cat coverage/lcov.info | ./node_modules/.bin/coveralls' # sends the coverage report to coveralls
